{
  "name": "@scrolled/sync-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Provider-agnostic sync contract: the SyncProvider interface, protocol types + zod schemas, the conflict handler, the SyncEngine state machine, and React status context/hooks on the /react subpath. Ships in every build. No network SDK.",
  "exports": {
    ".": "./src/index.ts",
    "./react": "./src/react.ts"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "peerDependencies": {
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.11",
    "react": "^18.3.1",
    "vitest": "^2.1.2"
  }
}
