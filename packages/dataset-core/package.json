{
  "name": "@scrolled/dataset-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared dataset types and Zod schemas: manifests, channels, and references for hosted datasets.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "vitest": "^2.1.2"
  }
}
