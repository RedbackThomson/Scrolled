{
  "name": "@scrolled/dataset-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Hosted-dataset distribution: manifest/channel schemas, the .scrolled-dataset container format, and the shared tar/gzip codec.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "vitest": "^2.1.2"
  }
}
