{
  "name": "@scrolled/mcp-protocol",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared wire-format types for the Scrolled MCP bridge. Pinned in one place so the browser and external Node processes can't drift.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "typescript": "^5.6.2",
    "vitest": "^2.1.2"
  }
}
