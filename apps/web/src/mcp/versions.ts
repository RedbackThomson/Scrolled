// Versions visible to MCP/CLI clients. Re-exported from the shared
// `@scrolled/mcp-protocol` package so the wire-format and tool-catalog
// versions are pinned in exactly one place.

export { PROTOCOL_VERSION, TOOL_VERSION } from '@scrolled/mcp-protocol';
