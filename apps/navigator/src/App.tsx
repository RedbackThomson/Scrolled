import { AppShell } from '@/components/layout/AppShell';
import { GraphCanvas } from '@/components/graph/GraphCanvas';

export function App() {
  return (
    <AppShell>
      <GraphCanvas />
    </AppShell>
  );
}
